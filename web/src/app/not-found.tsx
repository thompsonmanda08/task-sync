import { ErrorCard } from "@/components/base";

export default function NotFound() {
  return (
    <div className="flex items-center container h-screen justify-center m-auto">
      <ErrorCard />
    </div>
  );
}
